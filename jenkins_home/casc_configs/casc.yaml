unclassified:
  gitSCM:
    globalConfigName: "Jenkins CI"
    globalConfigEmail: "jenkins@example.com"
jenkins:
  systemMessage: "Jenkins configured automatically by Jenkins Configuration as Code plugin"
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "${ADMIN_PASSWORD}"
  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false
jobs:
  - script: |
      pipelineJob("my-public-github-repo-job") {
        description("Pipeline job pulling from a public GitHub repo")
        definition {
          cpsScm {
            scm {
              git("https://github.com/pintop9/jenkins-casc.git", "main")
            }
            scriptPath("Jenkinsfile")
          }
        }
      }